OBJ = determinant gauss lu_decomp matrix_example multiply multiply_dgemm pointer poisson_dense poisson_lu poisson_sparse random_matrix steepest_descent
default: $(OBJ)

CFLAGS = -O3
LDFLAGS = -llapack -lblas

clean:
	rm -rf $(OBJ)
